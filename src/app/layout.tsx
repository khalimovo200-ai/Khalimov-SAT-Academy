import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/contexts/AuthContext";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "ScorePath — Premium SAT Learning Platform",
  description: "Master the Digital SAT with adaptive learning, AI tutoring, and detailed analytics. Join thousands of students who improved their scores.",
  keywords: "SAT prep, digital SAT, SAT practice, SAT tutor, SAT courses",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
